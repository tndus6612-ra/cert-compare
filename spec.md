# 국가별 의료기기 인증 비교 웹사이트 - 설계 문서

## 1. 목적
RA팀이 국가별 의료기기 인증 소요기간 · 정부수수료(신규/변경/갱신 구분)를
한 화면에서 빠르게 비교할 수 있는 내부용 웹 대시보드를 만든다.
기존에는 엑셀 파일로 관리했으나, 팀원 누구나 브라우저에서 바로 보고
필터링할 수 있도록 웹으로 전환한다.

## 2. 데이터
- 이 폴더의 `cert_data.json`이 초기 시드 데이터다 (총 56개 항목).
- 각 항목의 필드:
  - `region` (권역: 아시아/유럽/중동/오세아니아/북미/중남미)
  - `country` (국가명)
  - `authority` (인증기관, 예: MFDS, FDA, Health Canada)
  - `applicationType` (신청유형: "신규" / "변경" 계열 / "갱신")
  - `productClass` (제품 등급/유형)
  - `monthsApprox` (심사기간 근사치, 개월 단위 숫자 또는 null)
  - `periodDescription` (심사기간 설명 텍스트, 예: "65 영업일")
  - `governmentFeeLocal` (정부 수수료, 현지화 표기 - USD 환산 없음)
  - `validity` (인증 유효기간)
  - `notes` (비고)
  - `source` (출처)
- 정부 수수료는 항상 **현지화 그대로** 표기한다. 서로 다른 국가 통화를
  임의로 환산해서 비교하지 않는다 (이전 버전에서 USD 환산을 넣었다가
  사용자 요청으로 제거한 이력이 있음 - 다시 넣지 말 것).

## 3. 화면 구성

### 3.1 "한눈에 보기" (메인 화면)
- 모든 항목(56개)을 지역별로 그룹핑해서 스크롤하며 훑어볼 수 있게 표시.
- 지역별로 시각적으로 구분되는 색상 블록 사용 (아시아/유럽/중동/오세아니아/북미/중남미 각각 다른 은은한 색).
- 같은 국가의 여러 행(신규/변경/갱신, 여러 등급)은 시각적으로 묶여서 보이게.
- `applicationType`은 배지(badge) 형태로 색 구분:
  - 신규 = 파랑 계열
  - 변경(모든 변경 유형 포함) = 주황 계열
  - 갱신 = 초록 계열
- 검색/필터 기능:
  - 국가명 검색
  - 지역 필터
  - 신청유형 필터 (신규/변경/갱신)
- 모바일에서도 깨지지 않고 보기 편해야 함 (반응형).

### 3.2 "상세 데이터" 화면 (또는 같은 화면 내 테이블 뷰 토글)
- 표 형태로 모든 필드를 보여주고, 헤더 클릭 시 정렬 가능.
- 컬럼별 필터(드롭다운) 가능하면 좋음.

### 3.3 추가 기능 (1차 배포 후 여유 있으면)
- 각 행에 "우리 팀 실제 경험" 메모를 추가/편집할 수 있는 기능
  (공개자료 추정치와 실제 경험치를 나란히 비교하기 위함).
  이 부분은 1차 버전에는 필수 아님 - 구조만 고려해서 나중에 필드 추가가
  쉽도록 설계.

## 4. 기술 스택 제안
- 별도 백엔드 없이 정적 사이트로 시작 (데이터는 `cert_data.json` 그대로 사용).
- React + Tailwind CSS 추천 (사용자가 이전에 Supabase + Netlify로
  라이선스 대시보드를 만든 경험이 있으므로, 나중에 "실제 경험" 편집 기능을
  넣을 때 Supabase를 붙이는 것도 자연스러운 확장 경로).
- 사용자는 코딩 경험이 전혀 없는 완전 초보자다. 클로드 코드가 작업할 때
  전문 용어보다는 쉬운 설명과 함께 진행해줄 것. 실행 방법(로컬 서버 켜는 법,
  배포하는 법)도 마지막에 아주 쉽게 안내해줄 것.

## 5. 우선순위
1. `cert_data.json`을 읽어서 "한눈에 보기" 화면 완성 (지역 그룹핑 + 배지 색 + 검색/필터)
2. 상세 테이블 뷰 (정렬 가능)
3. 반응형 스타일 다듬기
4. (여유 있으면) 로컬 실행 방법 + Netlify 배포 방법 안내
